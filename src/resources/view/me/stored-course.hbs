<div class="mt-4">
  <h3>Danh sách khóa học</h3>

  <table class="table mt-4">
    <thead>
      <tr>
        <th scope="col">#</th>
        <th scope="col">Tên sản phẩm</th>
        <th scope="col">HHH</th>
        <th scope="col">Thời gian</th>
      </tr>
    </thead>
    <tbody>
      {{#each sanpham}}
        <tr>
          <th scope="row">{{@index}}</th>
          <td>{{this.name}}</td>
          <td>{{this.price}}</td>
          <td>{{this.updateAt}}</td>
          <td><a
              href="/sanpham/{{this._id}}/edit"
              class="btn btn-link"
            >Sửa</a></td>
          <td><button
              type="button"
              class="btn btn-primary"
              data-toggle="modal" data-id="{{this._id}}"
              data-target="#delete-course-modal"
            >
              Xóa
            </button></td>

        </tr>
      {{/each}}

    </tbody>
  </table>
</div>

{{! delete }}
<div
  class="modal fade"
  id="delete-course-modal"
  tabindex="-1"
  role="dialog"
  aria-labelledby="exampleModalLabel"
  aria-hidden="true"
>
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Thông báo</h5>
        <button
          type="button"
          class="close"
          data-dismiss="modal"
          aria-label="Close"
        >
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        Bạn có chắc chắn muốn xóa khóa học này không? Nếu bạn xóa, tất cả dữ
        liệu liên quan đến khóa học này sẽ bị mất vĩnh viễn.
      </div>
      <div class="modal-footer">
        <button
          type="button"
          class="btn btn-secondary"
          data-dismiss="modal"
        >Đóng</button>
        <button id="btn-delete-course" type="button" class="btn btn-danger">Xóa</button>
      </div>
    </div>
  </div>
</div>

{{!-- form --}}
<form name="delete-course-form" method="POST" ></form>



<script>


document.addEventListener('DOMContentLoaded', function () {
var btnDeleteCourse = document.getElementById('btn-delete-course');
var deleteCourseForm = document.forms["delete-course-form"];
var sanphamId;

  $('#delete-course-modal').on('show.bs.modal', function (event) {
  var button = $(event.relatedTarget);
  sanphamId = button.data('id');
})

btnDeleteCourse.onclick = function () {
  console.log(sanphamId); // ← sửa đúng tên biến
  deleteCourseForm.action = '/sanpham/' + sanphamId + '?_method=DELETE';
  deleteCourseForm.submit();
}

});
</script>