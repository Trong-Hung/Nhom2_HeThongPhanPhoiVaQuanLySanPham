<h2>Đơn hàng đang vận chuyển</h2>

<ul>
  {{#each orders}}
    <li>
      Mã đơn: <a href="/shipper/order/{{this._id}}">{{this._id}}</a> 
      <br>📦 Kho xuất hàng: {{#if this.warehouseId}}{{this.warehouseId.name}}{{else}}❌ Chưa xác định{{/if}}  
      <br>📍 Địa chỉ kho: {{#if this.warehouseId}}{{this.warehouseId.address}}{{else}}❌ Chưa xác định{{/if}}  
      <br>🚚 Địa chỉ giao hàng: {{this.address}}  
      <br>🚀 Trạng thái: {{this.status}}

      <a href="/shipper/maps/{{this._id}}" class="btn btn-primary">Chỉ đường</a>
    </li>
  {{/each}}
</ul>
